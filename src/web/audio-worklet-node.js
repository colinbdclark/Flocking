// TODO: A wrapper around AudioWorkletNode that will:
//  * specify the appropriate inputs to the Node
//  * automatically create and connect ConstantSourceNodes to each of the
//    WorkletNode's inputs and store them by name so that it's easier to
//    target parameter changes into Flocking.
